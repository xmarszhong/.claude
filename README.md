# .claude

My personal Claude Code setup, including global rules and skills.

## Usage

```bash
curl -fsSL https://raw.githubusercontent.com/xmarszhong/.claude/main/sync.sh | bash
```

This command syncs the current repository's `main` branch into local `~/.claude`.

